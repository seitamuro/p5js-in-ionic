import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'p5js-in-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
